# Production WebSocket URL (set the same value in Vercel → Project → Environment Variables).
# After Belmo deploy, use the wss host Belmo gives you, e.g.:
# VITE_WS_URL=wss://your-service.app.belmo.io
#
# Leave unset for local dev (defaults to ws://localhost:2567).
VITE_WS_URL=
